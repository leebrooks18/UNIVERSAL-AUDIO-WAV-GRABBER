This branch was created to provide a stable preview environment for the project.

Use this branch for staging and pre-production testing. It contains no code changes — it's just a trigger commit to create the develop preview deployment on Vercel.
